spring:
  jpa:
    open-in-view: false
    show-sql: true
  banner:
    location: banner.txt
  r2dbc:
    url: ${HOST_DB:r2dbc:postgresql://localhost:5433/rest-test}
    username: ${DB_USER:admin}
    password: ${DB_USER:admin}
  flyway:
    url: ${HOST_DB:jdbc:postgresql://localhost:5433/rest-test}
    baselineOnMigrate: true
    enabled: true